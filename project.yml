name: Signal
options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    macOS: 14.0
settings:
  base:
    SWIFT_VERSION: 5.0
    PRODUCT_BUNDLE_IDENTIFIER: com.example.Signal
    GENERATE_INFOPLIST_FILE: YES
    INFOPLIST_KEY_NSHumanReadableCopyright: ""
    SDKROOT: macosx
    SUPPORTED_PLATFORMS: macosx
targets:
  Signal:
    type: application
    platform: macOS
    sources:
      - path: Signal
    settings:
      base:
        PRODUCT_NAME: Signal
        MACOSX_DEPLOYMENT_TARGET: 14.0
        SDKROOT: macosx
        SUPPORTED_PLATFORMS: macosx
